import type { MapBounds } from '../../types'

// Initial demo/product viewport only. It is not a permanent domain boundary.
export const DEMO_SPAIN_INITIAL_BOUNDS: MapBounds = {
  north: 44.5,
  south: 27.5,
  west: -18.5,
  east: 5,
}

export const GLOBAL_MAP_MAX_ZOOM = 15
